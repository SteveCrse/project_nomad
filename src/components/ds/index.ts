/**
 * Design-system primitives, ported from the Claude Design bundle
 * (_ds/project-nomad-design-system-1e4b4894.../_ds_bundle.js) to React + Tailwind.
 *
 * Only the primitives the test tool actually uses are ported so far. The
 * bundle also defines Dialog, Tooltip, Input, Select, Checkbox, GameCard and
 * ModuleSlot — port those when a screen needs them.
 */
export { Button } from './Button';
export { Badge } from './Badge';
export { Tabs } from './Tabs';
export { StatGauge } from './StatGauge';
export { DownsTracker } from './DownsTracker';
export { Toggle } from './Toggle';
